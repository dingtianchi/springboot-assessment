# How to demo in your local

### Backend API services
```shell
cd springboot-assessment
mvn clean install
mvn spring-boog:run
```

### Angular UI
```shell
cd springboot-assessment/ui
npm install
npm run build
ng serve --open
```